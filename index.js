import App from './App.js'; // Import the App component

const root = document.querySelector('#root'); // Grab the root div

// React will render the App component and attach it to the root div
ReactDOM.render(React.createElement(App), root);
